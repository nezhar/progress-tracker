services:
  progress-tracker:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    env_file:
      - .env
    volumes:
      - progress-data:/data
    restart: unless-stopped

volumes:
  progress-data:
